这里只描述UI，内部逻辑在 ../auth

页面路径： /login

管理员：

- 两个登录tab
- 支持手机号+密码登录（默认）
- 支持手机号+OTP登录

家长：

- 支持手机号+密码登录（默认）
- 支持手机号+OTP登录
- 支持儿童登录
  - 输入6位pin码，根据pin码验证身份是否存在以及是谁

所有登录都要限速，防止爆破，限速配置使用env配置，默认值为better-auth的默认值
